const tg=window.Telegram?.WebApp;
const SUPABASE_URL='https://fvuiisuzwezruxmlljty.supabase.co';
const SUPABASE_KEY='sb_publishable_NC5zaQusqVNFdoi3d8s9Ow_5wbtQF1a';
const REWARDS_API=`${SUPABASE_URL}/functions/v1/telegram-rewards`;
const state={balance:0,today:0,total:0,level:'Master',trust:50,streak:0};
if(tg){tg.ready();tg.expand();tg.setHeaderColor('#070a12');tg.setBackgroundColor('#070a12');}
const user=tg?.initDataUnsafe?.user;
const usernameEl=document.getElementById('username');if(usernameEl)usernameEl.textContent=user?.first_name||'Guest';
const avatarEl=document.getElementById('avatar');if(avatarEl)avatarEl.textContent=(user?.first_name?.[0]||'F').toUpperCase();
function render(){const b=document.getElementById('balance');if(b)b.textContent=state.balance.toLocaleString();const t=document.getElementById('today');if(t)t.textContent=state.today.toLocaleString();const total=document.getElementById('total');if(total)total.textContent=state.total.toLocaleString();const level=document.getElementById('level');if(level)level.textContent=state.level;const trust=document.getElementById('trustScore');if(trust)trust.textContent=`${state.trust}/100`;const streak=document.getElementById('dailyStreak');if(streak)streak.textContent=`${state.streak} days`;}
async function api(action,extra={}){if(!tg?.initData)throw new Error('Open this app from Telegram.');const r=await fetch(REWARDS_API,{method:'POST',headers:{'Content-Type':'application/json','apikey':SUPABASE_KEY},body:JSON.stringify({action,initData:tg.initData,...extra})});const data=await r.json();if(!r.ok||!data.ok)throw new Error(data.error||'API_ERROR');return data;}
async function loadBalance(){try{const d=await api('balance');state.balance=Number(d.wallet?.coins||0);const h=d.history||[];state.total=h.reduce((s,x)=>s+Number(x.points||0),0);state.today=h.filter(x=>new Date(x.created_at).getTime()>=Date.now()-86400000).reduce((s,x)=>s+Number(x.points||0),0);render();}catch(e){console.info('Balance:',e.message);}}
async function loadProfile(){try{const d=await api('profile');if(d.user){state.level=d.user.level||state.level;state.trust=Number(d.user.trust_score??state.trust);state.streak=Number(d.user.daily_streak??state.streak);render();}}catch(e){console.info('Profile:',e.message);}}
function popup(title,message){if(tg?.showPopup)tg.showPopup({title,message,buttons:[{id:'ok',type:'ok',text:'OK'}]});else alert(`${title}\n\n${message}`);}
async function invite(){const fallback=`https://t.me/frmoin_bot?start=ref_${user?.id||''}`;try{const d=await api('referral');const link=d.invite_link||fallback;const share=`https://t.me/share/url?url=${encodeURIComponent(link)}&text=${encodeURIComponent('Join FRMOIN Rewards and earn!')}`;try{await navigator.clipboard.writeText(link);}catch{}if(tg?.openTelegramLink)tg.openTelegramLink(share);else location.href=share;popup('Referral Link',`${link}\n\nYour invite link is ready.`);}catch(e){try{await navigator.clipboard.writeText(fallback);}catch{}popup('Referral Link',`${fallback}\n\nLink copied. Share it with your friends.`);}}
function waitForMonetag(ms=15000){return new Promise((res,rej)=>{const st=Date.now(),tm=setInterval(()=>{if(typeof show_11691259==='function'){clearInterval(tm);res(show_11691259);}else if(Date.now()-st>=ms){clearInterval(tm);rej(new Error('MONETAG_NOT_READY'));}},200);});}
async function rewardAfterAd(type,show){const s=document.getElementById('adStatus');if(s)s.textContent='Please complete the ad…';await show();const d=await api('reward',{adType:type});state.balance=Number(d.coins);state.total+=Number(d.points||0);state.today+=Number(d.points||0);render();if(s)s.textContent=`✅ +${d.points||0} coins added!`;}
const watch=document.getElementById('watchAd');if(watch)watch.addEventListener('click',async()=>{watch.disabled=true;try{await rewardAfterAd('rewarded_interstitial',await waitForMonetag());}catch(e){const s=document.getElementById('adStatus');if(s)s.textContent=`❌ ${e.message}`;}finally{watch.disabled=false;}});
document.querySelectorAll('.action-btn').forEach(btn=>btn.addEventListener('click',async()=>{const a=btn.dataset.action;if(a==='invite'){await invite();return;}if(a==='tasks'){location.href='tasks.html';return;}if(a==='daily'){popup('Daily Login','Daily login reward is available from the server.');return;}if(a==='withdraw'){popup('Withdraw',`Current balance: ${state.balance} coins\nMethods: bKash / USDT BEP20`);return;}if(a==='history'){popup('Reward History',`Total rewards: ${state.total} coins`);return;}popup('FRMOIN',`Selected: ${a||'option'}`);}));
loadBalance();loadProfile();render();